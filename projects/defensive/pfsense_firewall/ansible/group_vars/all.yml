---
# Host addresses referenced by the DHCP static maps, aliases and firewall rules.
# The full network layout (subnets, gateways, the Splunk indexer at 10.10.20.50)
# lives in ../../docs/topology.md and config/config.xml — the single source of
# truth — so it is not duplicated here as unused variables.

dc_ip: 10.10.10.10
wkstn_ip: 10.10.10.20
