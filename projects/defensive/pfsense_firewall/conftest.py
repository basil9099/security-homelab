# Presence of this file at the project root is what puts that root on sys.path
# under pytest's default (prepend) import mode, so tests import
# `config.sanitise_config` the same way the CLI is invoked. Intentionally empty.
