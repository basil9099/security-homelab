# Presence of this file at the project root is what puts that root on sys.path
# under pytest's default (prepend) import mode, so tests import `config`,
# `models`, `event_logger` … exactly as main.py does. Intentionally empty.
