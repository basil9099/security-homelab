# LLM Red-Team Framework — Python dependencies
# Install with: pip install -r requirements.txt

# The target application
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4

# HTTP client (Ollama backend, HTTP target client, FastAPI TestClient)
requests==2.32.3
httpx==0.28.1

# Framework mapping table
PyYAML==6.0.2

# Terminal output
rich==13.9.4

# ---------------------------------------------------------------------------
# garak is NOT listed here on purpose. It pulls in a large ML dependency tree
# (torch and friends) that this project does not import — the scanner drives the
# `garak` binary as an external tool. Install
# it separately when you want the garak engine:
#
#     pip install -r requirements-scan.txt
#
# Without it, `scan` still runs: garak families are skipped with a reason and the
# native packs carry on.
# ---------------------------------------------------------------------------
